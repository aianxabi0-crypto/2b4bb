// ==================== ФАЙЛ: next.config.js ====================
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com'], // Для заглушек изображений
  },
}

module.exports = nextConfig
